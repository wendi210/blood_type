import 'package:blood_type/landingpage.dart';
import 'package:flutter/material.dart';

void main() => runApp(
    MaterialApp(
      home: Landing(),
      theme: ThemeData(
        unselectedWidgetColor:Colors.grey)
    )
);